apiVersion: serving.kubeflow.org/v1
kind: InferenceService
metadata:
  name: endpoint-name
spec:
  predictor:
    sklearn:
      storageUri: "s3://your-model-bucket/model.pth"
  default:
    predictorName: sklearn-model
    minReplicas: 1
    maxReplicas: 1
